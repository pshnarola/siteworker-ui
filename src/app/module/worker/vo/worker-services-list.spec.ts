import { WorkerServicesList } from './worker-services-list';

describe('WorkerServicesList', () => {
  it('should create an instance', () => {
    expect(new WorkerServicesList()).toBeTruthy();
  });
});
