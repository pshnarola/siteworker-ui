import { JobReimbursementAttachmentDTO } from './job-reimbursement-attachment-dto';

describe('JobReimbursementAttachmentDTO', () => {
  it('should create an instance', () => {
    expect(new JobReimbursementAttachmentDTO()).toBeTruthy();
  });
});
