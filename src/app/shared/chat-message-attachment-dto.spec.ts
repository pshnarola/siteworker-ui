import { ChatMessageAttachmentDTO } from './chat-message-attachment-dto';

describe('ChatMessageAttachmentDTO', () => {
  it('should create an instance', () => {
    expect(new ChatMessageAttachmentDTO()).toBeTruthy();
  });
});
