import { MarkAsFavouriteParam } from './mark-as-favourite-param';

describe('MarkAsFavouriteParam', () => {
  it('should create an instance', () => {
    expect(new MarkAsFavouriteParam()).toBeTruthy();
  });
});
